export class GeneralPage {

    static screenShootsTaken = [];

    static stepScreenshot(step, seg = 1000) {
        // cy.wait(seg);
        // let fileName = `step0${step}`;
        // if (!GeneralPage.screenShootsTaken.includes(fileName)) {
        //     cy.screenshot(fileName);
        //     GeneralPage.screenShootsTaken.push(fileName);
        // }
    }
    
}